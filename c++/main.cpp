#include <iostream>
#include <vector>
#include <string>

using namespace std;
int main() {

    double one = 12.0, two = 11.9999999999999999;
    //cin >> one >> two;
    cout << (one == two);
    return 0;
}